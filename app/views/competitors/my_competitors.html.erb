<%= render 'shared/banner' %>
<div class="container">
  <div class="row"></div>
    <div id="add-competitor">
      <h3 class=text-center>Add a Competitor</h3>
      <%= render "form" %>
    </div>
  </div>
</div>
<% if @user.competitors.empty? %>
  <div class="container">
    <p class="text-center">Add a new competitor</p>
  </div>
<% else %>
  <%= render "index" %>
<% end %>
