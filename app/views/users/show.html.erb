<%= render 'shared/banner' %>
<div class="container">
  <div class="row"></div>
    <div class="col-xs-12 col-sm-6 col-sm-offset-3 col-md-8 col-md-offset-2">
      <div class="activity">
        <% if @user.competitors.empty? %>
          <p class="text-center">Add a new competitor</p>
        <% else %>
          <%= render 'feed' %>
        <% end %>
      </div>
    </div>
  </div>
</div>
